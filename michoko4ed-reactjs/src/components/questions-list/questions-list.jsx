import React from 'react';
import PropTypes from 'prop-types';
import styles from './Questions-list.module.scss';

const Questions-list = () => (
  <div className={styles.Questions-list} data-testid="Questions-list">
    Questions-list Component
  </div>
);

Questions-list.propTypes = {};

Questions-list.defaultProps = {};

export default Questions-list;
